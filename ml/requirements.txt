# --- Serving (what the deployed container actually needs) ---
# onnxruntime runs the exported model on CPU. No torch, no GPU, no CUDA.
onnxruntime>=1.17
numpy>=1.24
Pillow>=10.0
opencv-python-headless>=4.8   # headless: no GUI libs, keeps the image small

# --- Training only (not needed at serving time) ---
# Install with:  pip install -r requirements-train.txt
