# Training data - fetch with `python src/prepare_data.py`
data/

# Training checkpoints and experiment artifacts stay out of git.
*.pt
*.pth
models/mobilenetv2_imagenet.onnx
models/efficientnet_lite4_imagenet.onnx
models/hazard_classifier_effb0.onnx

# The served model IS committed: Code Engine builds from source, so the image
# build needs it present in the repo. It is 8.9MB.
!models/hazard_classifier.onnx
!models/classes.json

__pycache__/
*.pyc
outputs/train*.log
